# Block all direct web access to the Laravel application folder.
# Only the front controller (../index.php) may load it.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
